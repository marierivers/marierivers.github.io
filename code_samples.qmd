---
title: "Code Samples"
listing: 
  contents: code_samples # all Quarto docs in the `code_samples` directory will be included on your listing page
  type: grid # or `default` ` grid`, or `table`; each type has its own set of yaml options to include
  sort: "date desc" # can also sort on more than one field
  categories: true # allows you to sort posts by assigned categories 
---